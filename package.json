{
  "name": "m2v",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm --workspace @m2v/core test",
    "sync:fec": "node pipeline/fec-sync.mjs",
    "app": "npm --workspace m2v-mobile start"
  }
}
